"""Лимиты Telegram, применяемые при отправке сообщений ботом.

Telegram ограничивает подпись к медиа (caption) 1024 символами. Когда к рассылке
или ответу в чате приложен файл, текст уходит как caption, и превышение приводит
к ошибке отправки, которую бот ловит молча. Поэтому валидируем длину на backend
и отдаём явный 400 (а не тихо обрезаем/теряем сообщение).
"""

# Должно совпадать с MESSAGE_MAX_LENGTH во фронтенде (admin_miniapp/src/constants.js).
MESSAGE_MAX_LENGTH = 1024
