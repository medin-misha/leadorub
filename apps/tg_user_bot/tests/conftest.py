"""Общее тестовое окружение.

Настройки процесса (`app.core.settings`) читаются на импорте модулей и требуют
TOKEN. conftest импортируется pytest'ом раньше любых тестовых модулей, поэтому
подставляем фиктивный токен здесь — независимо от порядка сбора тестов.
"""

import os

os.environ.setdefault("TOKEN", "123456:test-token")
